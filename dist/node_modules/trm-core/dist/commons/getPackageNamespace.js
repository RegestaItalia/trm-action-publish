"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPackageNamespace = void 0;
function getPackageNamespace(devclass) {
    const aDevclass = Array.from(devclass.toUpperCase());
    if (aDevclass[0] === 'Z') {
        return 'Z';
    }
    else if (aDevclass[0] === 'Y') {
        return 'Y';
    }
    else if (aDevclass[0] === '$') {
        return '$';
    }
    else if (/^(\/.*\/)/.test(devclass)) {
        return devclass.toUpperCase().match(/^(\/.*\/)/)[1];
    }
    else {
        throw new Error(`Devclass ${devclass.toUpperCase()} uses a non-custom namespace.`);
    }
}
exports.getPackageNamespace = getPackageNamespace;
