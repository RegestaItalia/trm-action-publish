import { Registry } from "../registry";
import { TrmPackage } from "../trmPackage";
export declare function installDependency(data: {
    packageName: string;
    versionRange: string;
    integrity: string;
    installedPackages?: TrmPackage[];
    originalInstallOptions?: any;
}, registry: Registry): Promise<void>;
