"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.releaseTadirTr = void 0;
const logger_1 = require("../../logger");
exports.releaseTadirTr = {
    name: 'release-tadir-tr',
    run: (context) => __awaiter(void 0, void 0, void 0, function* () {
        const tmpFolder = context.parsedInput.releaseFolder;
        const timeout = context.parsedInput.releaseTimeout;
        yield context.runtime.tadirTransport.release(false, false, tmpFolder, timeout);
        logger_1.Logger.log(`TADIR released, setting try revert to false as it cannot be deleted`, true);
        context.runtime.tryTadirDeleteRevert = false;
    })
};
