"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkPublishAllowed = void 0;
const logger_1 = require("../../logger");
exports.checkPublishAllowed = {
    name: 'check-publish-allowed',
    run: (context) => __awaiter(void 0, void 0, void 0, function* () {
        const packageName = context.parsedInput.packageName;
        logger_1.Logger.loading(`Checking publish authorization...`);
        const oDummyTrmPackage = context.runtime.dummyPackage;
        var publishAllowed = true;
        try {
            publishAllowed = yield oDummyTrmPackage.canPublishReleases();
        }
        catch (e) {
        }
        if (!publishAllowed) {
            throw new Error(`You are not not authorized to publish "${packageName}" releases.`);
        }
        else {
            logger_1.Logger.success(`Package check successful.`);
        }
    })
};
