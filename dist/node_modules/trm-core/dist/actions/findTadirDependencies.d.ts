import { Logger } from "../logger";
import { DEVCLASS } from "../rfc/components";
import { TADIR } from "../rfc/struct";
import { SystemConnector } from "../systemConnector";
import { TrmPackage } from "../trmPackage";
export type TadirDependency = {
    trmPackage?: TrmPackage;
    isSap: boolean;
    integrity?: string;
    tadir: TADIR[];
};
export declare function findTadirDependencies(data: {
    devclass: DEVCLASS;
    tadir?: TADIR[];
}, system: SystemConnector, logger?: Logger): Promise<TadirDependency[]>;
