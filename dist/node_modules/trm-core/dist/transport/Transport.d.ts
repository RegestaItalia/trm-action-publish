/// <reference types="node" />
import { Logger } from "../logger";
import { SystemConnector } from "../systemConnector";
import { AS4TEXT, DEVCLASS, PGMID, SOBJ_NAME, TRKORR, TROBJTYPE, TROBJ_NAME, TR_TARGET } from "../rfc/components";
import { BinaryTransport } from "./BinaryTransport";
import { FileNames } from "./FileNames";
import { FilePaths } from "./FilePaths";
import { TransportContent } from "./TransportContent";
import { E070, E071 } from "../rfc/struct";
import { Documentation } from "./Documentation";
import { TrmTransportIdentifier } from "./TrmTransportIdentifier";
import { TrmPackage } from "../trmPackage";
export declare const COMMENT_OBJ: TROBJTYPE;
export declare class Transport {
    trkorr: TRKORR;
    private _systemConnector;
    private _trTarget?;
    private _logger?;
    private _fileNames;
    private _e070;
    private _e071;
    private _docs;
    trmIdentifier?: TrmTransportIdentifier;
    constructor(trkorr: TRKORR, _systemConnector: SystemConnector, _trTarget?: TR_TARGET, _logger?: Logger);
    setTrmIdentifier(identifier?: TrmTransportIdentifier): Transport;
    getE070(): Promise<E070>;
    getE071(): Promise<E071[]>;
    getDevclass(): Promise<DEVCLASS>;
    getDate(): Promise<Date>;
    isTrmRelevant(): Promise<boolean>;
    download(skipLog?: boolean): Promise<{
        binaries: BinaryTransport;
        filenames: FileNames;
    }>;
    setDocumentation(sDocumentation: string, skipLog?: boolean): Promise<Transport>;
    getDocumentation(skipLog?: boolean): Promise<Documentation[]>;
    static doktlToDoc(doktl: {
        langu: string;
        dokversion: string;
        line: string;
        doktext: string;
    }[]): Documentation[];
    addObjects(objects: E071[], lock: boolean): Promise<void>;
    addComment(comment: TROBJ_NAME): Promise<void>;
    getLinkedPackage(): Promise<TrmPackage>;
    delete(): Promise<null>;
    release(lock: boolean, skipLog?: boolean, tmpFolder?: string, secondsTimeout?: number): Promise<Transport>;
    readReleaseLog(tmpFolder: string, secondsTimeout: number): Promise<void>;
    readImportLog(tmpFolder: string): Promise<void>;
    private _isInTmsQueue;
    private static _getFileNames;
    static _getFilePaths(fileNames: FileNames, systemConnector: SystemConnector, logger?: Logger): Promise<FilePaths>;
    static createToc(data: {
        text: AS4TEXT;
        target: TR_TARGET;
        trmIdentifier?: TrmTransportIdentifier;
    }, systemConnector: SystemConnector, skipLog?: boolean, logger?: Logger): Promise<Transport>;
    static createWb(data: {
        text: AS4TEXT;
        target?: TR_TARGET;
    }, systemConnector: SystemConnector, skipLog?: boolean, logger?: Logger): Promise<Transport>;
    static getContent(data: Buffer, tmpFolder: string): Promise<TransportContent>;
    static upload(data: {
        binary: BinaryTransport;
        systemConnector: SystemConnector;
        tmpFolder?: string;
        trTarget?: TR_TARGET;
    }, skipLog?: boolean, logger?: Logger): Promise<Transport>;
    static getTransportsFromObject(objectKeys: {
        pgmid: PGMID;
        object: TROBJTYPE;
        objName: SOBJ_NAME;
    }, systemConnector: SystemConnector): Promise<Transport[]>;
    static getLatest(transports: Transport[]): Promise<Transport>;
    import(skipLog?: boolean, timeout?: number): Promise<void>;
    rename(as4text: string): Promise<void>;
}
